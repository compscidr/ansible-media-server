---
- name: Ensure docker is installed on the target system
  tags: docker
  ansible.builtin.include_role:
    name: nickjj.docker
    apply:
      become: true # without this, it will end up failing and retrying over and over to install deps
  when: sonarr_install_docker

- name: Create Sonarr Directories
  tags: sonarr
  become: true
  ansible.builtin.file:
    path: "{{ sonarr_folder }}"
    state: directory
    mode: "755"
    owner: root
    group: root

- name: Deploy sonarr
  tags: sonarr
  become: true
  community.docker.docker_container:
    name: sonarr
    image: lscr.io/linuxserver/sonarr:latest
    pull: true
    ports:
      - "{{ sonarr_port }}:8989"
    volumes:
      - "{{ sonarr_tv_folder }}:/tv:rw"
      - "{{ sonarr_transmissions_downloads_folder }}:/downloads:rw"
      - "{{ sonarr_sabnzbd_downloads_folder }}:/downloads/complete:rw"
      - "{{ sonarr_folder }}:/config:rw"
    env:
      TZ: "{{ sonarr_tz }}"
      PUID: "{{ sonarr_pid }}"
      PGID: "{{ sonarr_gid }}"
    restart_policy: unless-stopped
    memory: "{{ sonarr_memory }}"
    memory_swap: "{{ sonarr_memory_swap }}"
